{
  "scripts": {
    "build": "cd frontend && yarn build && cd ../backend && yarn build && cd .. && node buildForProduction.js"
  },
  "devDependencies": {
    "ncp": "^2.0.0",
    "rimraf": "^3.0.2"
  }
}
